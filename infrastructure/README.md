# Infraestrutura

Ainda não implementado. Próximo passo planejado: Terraform (ou AWS CDK) provisionando o
backend como Lambda + API Gateway, servindo o front-end estático via S3 + CloudFront — a
mesma arquitetura que a ferramenta recomendaria pra ela mesma.
